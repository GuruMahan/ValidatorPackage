import XCTest
@testable import ValidatorPackage

final class ValidatorPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ValidatorPackage().text, "Hello, World!")
    }
}
